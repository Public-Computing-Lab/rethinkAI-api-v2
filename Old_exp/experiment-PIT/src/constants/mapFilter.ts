/**
 * mapFilter.tsx
 * This file holds the typescript interface of the filter used in the map-chat-link functionality
 * all property are optional
 */

export interface mapFilter {
    year?: number[],
    data_type?: string[],
    location?: number[]
}